// Quarantined implementation remains in git history.
export function ruleazaPipelineComplet() { throw new Error("Legacy pipeline retired. Run npm run crawl:verified."); }
if (process.argv[1]?.endsWith("run-pipeline.mjs")) ruleazaPipelineComplet();
