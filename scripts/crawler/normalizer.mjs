// Retired: legacy inference and confidence scores did not prove source evidence.
export function curataTextTitlu() { throw new Error("Legacy crawler retired. Use npm run crawl:census and crawl:report; see scripts/crawler/README.md."); }
export function detecteazaSenioritate() { throw new Error("Legacy crawler retired. Use npm run crawl:census and crawl:report; see scripts/crawler/README.md."); }
export function normalizeazaOcupatie() { throw new Error("Legacy crawler retired. Use npm run crawl:census and crawl:report; see scripts/crawler/README.md."); }
