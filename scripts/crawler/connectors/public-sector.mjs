// Quarantined implementation remains in git history.
export function extrageObservatiiSectorPublic() { throw new Error("Legacy public connector disabled: aggregate grid ranges cannot be counted as employees. Use the documented public grid datasets separately."); }
